all:
  children:
    
    #serveur RHEL
    common:
      children:
        
        nginx:
          hosts:
            172.19.0.2:

        webserver:
          hosts:
            172.19.0.3:
  
  vars:
    #ansible_python_interpreter: /usr/bin/python3
    ansible_ssh_pass: rootpass
